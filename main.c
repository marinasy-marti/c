#include <stdio.h>

int main()
{
    int age;

    // printf("Enter your age: ");
    // scanf("%d", &age);

    // printf("You are %d years old\n", age);

    // this is a comment
    /* this
        is
        a
        multiline
        comment
    */
    return 0;
}